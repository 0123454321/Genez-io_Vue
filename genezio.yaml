frontend:
    path: .
    publish: dist
    scripts:
        build: npm run build
        deploy: npm install
        start:
            - npm install --silent
            - npm run dev --silent
    subdomain: vue-learn
name: vue-learn
region: us-east-1
yamlVersion: 2
